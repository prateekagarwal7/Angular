import { Component } from '@angular/core';

@Component({
  selector: 'app-login',// this will work as the html tag  we can use this selector invoke the component in parent component
  standalone: true,
  imports: [],
  templateUrl: './login.component.html',
  styleUrl: './login.component.css'
})
export class LoginComponent {

}
